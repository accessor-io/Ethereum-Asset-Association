import ENSRegistrarABI from '../contracts/abi/ENSRegistrar.json';
import PublicResolverABI from '../contracts/abi/PublicResolver.json';

const ABIs = {
  ENS_REGISTRAR_ABI: ENSRegistrarABI,
  PUBLIC_RESOLVER_ABI: PublicResolverABI
};

export default ABIs;

// You can add more ABIs here if needed